강원 화물협회 자동화 v2.2 Compact 배포

1. 이 폴더의 모든 파일을 기존 GitHub 저장소 최상위에 덮어씁니다.
2. 삭제된 파일이 있으면 GitHub에서도 같이 삭제합니다.
3. 커밋 메시지: v2.2 업무순서형 UI 재설계
4. main 브랜치에 Push 합니다.
5. Railway 배포가 끝난 뒤 /messages/arrears/ 화면에서 새 UI를 확인합니다.
6. 브라우저가 옛 CSS를 보이면 Ctrl+F5로 강력 새로고침합니다.

주요 변경:
- 미수금 문자 화면 2열 구성
- 대상 선택 → 발송 설정 → 최종 확인 3단계
- 긴 가로 입력창 제거
- 현재 선택 인원 실시간 표시
- 즉시/예약 발송 카드 선택
- 화면 전체 폭 1180px 제한
